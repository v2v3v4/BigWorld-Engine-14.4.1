#include "WeatherSystemGameLogic.hpp"


// -----------------------------------------------------------------------------
// Section: ClientMethod implementations 
// -----------------------------------------------------------------------------// -----------------------------------------------------------------------------
// Section: Property setter callbacks
// -----------------------------------------------------------------------------

/**
 *	This method implements the setter callback for the property name.
 */
void WeatherSystemGameLogic::set_name( const BW::string & oldValue )
{
	// DEBUG_MSG( "WeatherSystemGameLogic::set_name\n" );
}

/**
 *	This method implements the setter callback for the property radius.
 */
void WeatherSystemGameLogic::set_radius( const float & oldValue )
{
	// DEBUG_MSG( "WeatherSystemGameLogic::set_radius\n" );
}

// WeatherSystemGameLogic.mm
