#include "AlertBeastTriggerGameLogic.hpp"


// -----------------------------------------------------------------------------
// Section: ClientMethod implementations 
// -----------------------------------------------------------------------------// -----------------------------------------------------------------------------
// Section: Property setter callbacks
// -----------------------------------------------------------------------------

/**
 *	This method implements the setter callback for the property trapRadius.
 */
void AlertBeastTriggerGameLogic::set_trapRadius( const float & oldValue )
{
	// DEBUG_MSG( "AlertBeastTriggerGameLogic::set_trapRadius\n" );
}

/**
 *	This method implements the setter callback for the property findBeastRadius.
 */
void AlertBeastTriggerGameLogic::set_findBeastRadius( const float & oldValue )
{
	// DEBUG_MSG( "AlertBeastTriggerGameLogic::set_findBeastRadius\n" );
}

// AlertBeastTriggerGameLogic.mm
