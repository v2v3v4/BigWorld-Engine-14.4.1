#include "FogGameLogic.hpp"


// -----------------------------------------------------------------------------
// Section: ClientMethod implementations 
// -----------------------------------------------------------------------------// -----------------------------------------------------------------------------
// Section: Property setter callbacks
// -----------------------------------------------------------------------------

/**
 *	This method implements the setter callback for the property density.
 */
void FogGameLogic::set_density( const BW::int8 & oldValue )
{
	// DEBUG_MSG( "FogGameLogic::set_density\n" );
}

/**
 *	This method implements the setter callback for the property innerRadius.
 */
void FogGameLogic::set_innerRadius( const float & oldValue )
{
	// DEBUG_MSG( "FogGameLogic::set_innerRadius\n" );
}

/**
 *	This method implements the setter callback for the property outerRadius.
 */
void FogGameLogic::set_outerRadius( const float & oldValue )
{
	// DEBUG_MSG( "FogGameLogic::set_outerRadius\n" );
}

/**
 *	This method implements the setter callback for the property red.
 */
void FogGameLogic::set_red( const BW::uint8 & oldValue )
{
	// DEBUG_MSG( "FogGameLogic::set_red\n" );
}

/**
 *	This method implements the setter callback for the property green.
 */
void FogGameLogic::set_green( const BW::uint8 & oldValue )
{
	// DEBUG_MSG( "FogGameLogic::set_green\n" );
}

/**
 *	This method implements the setter callback for the property blue.
 */
void FogGameLogic::set_blue( const BW::uint8 & oldValue )
{
	// DEBUG_MSG( "FogGameLogic::set_blue\n" );
}

// FogGameLogic.mm
