#include "BeastGameLogic.hpp"


// -----------------------------------------------------------------------------
// Section: ClientMethod implementations 
// -----------------------------------------------------------------------------

/**
 *	This method implements the ClientMethod initiateRage.
 */
void BeastGameLogic::initiateRage(  )
{
	// DEBUG_MSG( "BeastGameLogic::set_initiateRage\n" );
}

// -----------------------------------------------------------------------------
// Section: Property setter callbacks
// -----------------------------------------------------------------------------

/**
 *	This method implements the setter callback for the property lookAtTarget.
 */
void BeastGameLogic::set_lookAtTarget( const BW::int32 & oldValue )
{
	// DEBUG_MSG( "BeastGameLogic::set_lookAtTarget\n" );
}

/**
 *	This method implements the setter callback for the property effectRadius.
 */
void BeastGameLogic::set_effectRadius( const float & oldValue )
{
	// DEBUG_MSG( "BeastGameLogic::set_effectRadius\n" );
}

/**
 *	This method implements the setter callback for the property dustTriggerRadius.
 */
void BeastGameLogic::set_dustTriggerRadius( const float & oldValue )
{
	// DEBUG_MSG( "BeastGameLogic::set_dustTriggerRadius\n" );
}

// BeastGameLogic.mm
