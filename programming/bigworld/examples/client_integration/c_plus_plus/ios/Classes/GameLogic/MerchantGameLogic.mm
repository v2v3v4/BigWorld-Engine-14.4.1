#include "MerchantGameLogic.hpp"


// -----------------------------------------------------------------------------
// Section: ClientMethod implementations 
// -----------------------------------------------------------------------------

/**
 *	This method implements the ClientMethod chat.
 */
void MerchantGameLogic::chat( 
			const BW::string & message )
{
	// DEBUG_MSG( "MerchantGameLogic::set_chat\n" );
}

// -----------------------------------------------------------------------------
// Section: Property setter callbacks
// -----------------------------------------------------------------------------

/**
 *	This method implements the setter callback for the property modeTarget.
 */
void MerchantGameLogic::set_modeTarget( const BW::int32 & oldValue )
{
	// DEBUG_MSG( "MerchantGameLogic::set_modeTarget\n" );
}

/**
 *	This method implements the setter callback for the property cancelTradeRadius.
 */
void MerchantGameLogic::set_cancelTradeRadius( const float & oldValue )
{
	// DEBUG_MSG( "MerchantGameLogic::set_cancelTradeRadius\n" );
}

// MerchantGameLogic.mm
