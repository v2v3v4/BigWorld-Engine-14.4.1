#include "LandmarkGameLogic.hpp"


// -----------------------------------------------------------------------------
// Section: ClientMethod implementations 
// -----------------------------------------------------------------------------// -----------------------------------------------------------------------------
// Section: Property setter callbacks
// -----------------------------------------------------------------------------

/**
 *	This method implements the setter callback for the property initialTriggerDelay.
 */
void LandmarkGameLogic::set_initialTriggerDelay( const BW::int8 & oldValue )
{
	// DEBUG_MSG( "LandmarkGameLogic::set_initialTriggerDelay\n" );
}

/**
 *	This method implements the setter callback for the property retriggerDelay.
 */
void LandmarkGameLogic::set_retriggerDelay( const BW::uint8 & oldValue )
{
	// DEBUG_MSG( "LandmarkGameLogic::set_retriggerDelay\n" );
}

/**
 *	This method implements the setter callback for the property radius.
 */
void LandmarkGameLogic::set_radius( const float & oldValue )
{
	// DEBUG_MSG( "LandmarkGameLogic::set_radius\n" );
}

/**
 *	This method implements the setter callback for the property description.
 */
void LandmarkGameLogic::set_description( const BW::string & oldValue )
{
	// DEBUG_MSG( "LandmarkGameLogic::set_description\n" );
}

// LandmarkGameLogic.mm
