#include "ReverbGameLogic.hpp"


// -----------------------------------------------------------------------------
// Section: ClientMethod implementations 
// -----------------------------------------------------------------------------// -----------------------------------------------------------------------------
// Section: Property setter callbacks
// -----------------------------------------------------------------------------

/**
 *	This method implements the setter callback for the property presetName.
 */
void ReverbGameLogic::set_presetName( const BW::string & oldValue )
{
	// DEBUG_MSG( "ReverbGameLogic::set_presetName\n" );
}

/**
 *	This method implements the setter callback for the property innerRadius.
 */
void ReverbGameLogic::set_innerRadius( const float & oldValue )
{
	// DEBUG_MSG( "ReverbGameLogic::set_innerRadius\n" );
}

/**
 *	This method implements the setter callback for the property outerRadius.
 */
void ReverbGameLogic::set_outerRadius( const float & oldValue )
{
	// DEBUG_MSG( "ReverbGameLogic::set_outerRadius\n" );
}

// ReverbGameLogic.mm
