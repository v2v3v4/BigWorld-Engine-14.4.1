#include "DroppedItemGameLogic.hpp"


// -----------------------------------------------------------------------------
// Section: ClientMethod implementations 
// -----------------------------------------------------------------------------// -----------------------------------------------------------------------------
// Section: Property setter callbacks
// -----------------------------------------------------------------------------

/**
 *	This method implements the setter callback for the property classType.
 */
void DroppedItemGameLogic::set_classType( const BW::int32 & oldValue )
{
	// DEBUG_MSG( "DroppedItemGameLogic::set_classType\n" );
}

/**
 *	This method implements the setter callback for the property itemSerial.
 */
void DroppedItemGameLogic::set_itemSerial( const BW::int32 & oldValue )
{
	// DEBUG_MSG( "DroppedItemGameLogic::set_itemSerial\n" );
}

/**
 *	This method implements the setter callback for the property onGround.
 */
void DroppedItemGameLogic::set_onGround( const BW::int8 & oldValue )
{
	// DEBUG_MSG( "DroppedItemGameLogic::set_onGround\n" );
}

/**
 *	This method implements the setter callback for the property dropperID.
 */
void DroppedItemGameLogic::set_dropperID( const BW::int32 & oldValue )
{
	// DEBUG_MSG( "DroppedItemGameLogic::set_dropperID\n" );
}

// DroppedItemGameLogic.mm
