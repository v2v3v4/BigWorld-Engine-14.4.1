#include "pch.hpp"

#include "triangle.hpp"


BW_BEGIN_NAMESPACE

using namespace VisualManipulator;

/*bool Triangle::operator ==( const Triangle& triangle)
{
	return a == triangle.a &&
		b == triangle.b &&
		c == triangle.c &&
		materialIndex == triangle.materialIndex;
}

bool Triangle::operator <( const Triangle& triangle)
{
	if (a < triangle.a) return true;
	if (a > triangle.a) return false;

	if (b < triangle.b) return true;
	if (b > triangle.b) return false;

	if (c < triangle.c) return true;
	if (c > triangle.c) return false;

	if (materialIndex < triangle.materialIndex) return true;

	return false;

}*/

BW_END_NAMESPACE
